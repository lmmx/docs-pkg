{
  "buildCommand": "python3 -m pdm run mkdocs build",
  "outputDirectory": "site",
  "installCommand": "yum install openssl && python3 --version && python3 -m pip install pdm 'urllib3<2' && python3 -m pdm install -v && python3 -m pdm run mkdocs"
}
