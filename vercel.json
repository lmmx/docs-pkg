{
  "buildCommand": "python3 -m pdm run mkdocs build",
  "outputDirectory": "site",
  "installCommand": "sh install_openssl.sh && python3 --version && python3 -m pip install pdm && python3 -m pdm install -v && ls -a && python3 -m pdm run mkdocs"
}
