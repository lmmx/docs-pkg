{
  "buildCommand": "python3 -m pdm run mkdocs build",
  "outputDirectory": "site",
  "installCommand": "python3 --version && python3 -m pip install -v -r docs/reqs/vercel.txt && python3 -m mkdocs"
}
