{
  "buildCommand": "python3 -m pdm run mkdocs build",
  "outputDirectory": "site",
  "installCommand": "amazon-linux-extras install epel && yum install openssl && python3 --version && python3 -m pip install pdm 'urllib3<2' && python3 -m pdm install -v && ls -a && python3 -m pdm run mkdocs"
}
